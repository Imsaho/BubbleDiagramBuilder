{% extends 'base.html.twig' %}

{% block page_header %}Budynek{% endblock page_header %}
{% block small_header %}dodaj{% endblock small_header %}

{% block body %}

    <form action="{{ path('building_new') }}" method="POST" novalidate="novalidate">
{#        {{ form_errors(form.name) }}#}

        {{ form_row(form.name) }}
        {{ form_row(form.NBAFactor) }}
        {{ form_row(form.coordinates) }}
        {{ form_row(form.description) }}
        {{ form_row(form.team) }}

        {{ form_rest(form) }}

        <input type="submit" value="Create" />

    </form>

    <ul>
        <li>
            <a href="{{ path('building_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
