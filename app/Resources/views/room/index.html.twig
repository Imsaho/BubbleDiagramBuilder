{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rooms list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Number</th>
                <th>Name</th>
                <th>Area</th>
                <th>Minheight</th>
                <th>Requirements</th>
                <th>Description</th>
                <th>Maxpersonamount</th>
                <th>Accesscontrol</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for room in rooms %}
            <tr>
                <td><a href="{{ path('room_show', { 'id': room.id }) }}">{{ room.id }}</a></td>
                <td>{{ room.number }}</td>
                <td>{{ room.name }}</td>
                <td>{{ room.area }}</td>
                <td>{{ room.minHeight }}</td>
                <td>{{ room.requirements }}</td>
                <td>{{ room.description }}</td>
                <td>{{ room.maxPersonAmount }}</td>
                <td>{% if room.accessControl %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('room_show', { 'id': room.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('room_edit', { 'id': room.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('room_new') }}">Create a new room</a>
        </li>
    </ul>
{% endblock %}
