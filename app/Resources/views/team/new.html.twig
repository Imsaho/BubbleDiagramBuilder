{% extends 'base.html.twig' %}

{% block page_header %}Team{% endblock page_header %}
{% block small_header %}dodaj{% endblock small_header %}

{% block body %}

    <form action="{{ path('team_new') }}" method="POST" novalidate="novalidate">

        {{ form_row(form.name, {'label': 'Nazwa'}) }}
        {{ form_row(form.users, {'label': 'Użytkownicy'}) }}

        {{ form_rest(form) }}

        <input type="submit" value="Utwórz" class="btn btn-primary btn-lg btn-block" />

    </form>

    <br/>

    <a href="{{ path('team_index') }}" class="btn btn-lg">Powrót do listy</a>

{% endblock %}
